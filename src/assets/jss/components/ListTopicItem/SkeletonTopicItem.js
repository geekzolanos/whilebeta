export default theme => ({
    duration: {
        marginBottom: theme.spacing(2)
    },
    root: {
        padding: theme.spacing(4,2),
        opacity: 1
    },
    heading: {
        marginTop: theme.spacing(-1)
    },
    goIcon: {
        height: '100%',
        fontSize: '2.4em'
    }
});